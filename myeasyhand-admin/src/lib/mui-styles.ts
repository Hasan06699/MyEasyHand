/** Re-export MUI styles — avoids Turbopack subpath resolution issues with @mui/material/styles */
export {
  ThemeProvider,
  createTheme,
  styled,
  useTheme,
  alpha,
  adaptV4Theme,
} from '@mui/material/styles';
